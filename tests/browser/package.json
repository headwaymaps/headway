{
  "name": "headway-browser-tests",
  "private": true,
  "description": "Playwright UI tests driving the Headway frontend against a running stack.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
