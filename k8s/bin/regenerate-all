#!/bin/bash

set -e
set -o pipefail

REPO_ROOT=$(git rev-parse --show-toplevel)

cd "$REPO_ROOT/k8s"

HEADWAY_CONTAINER_TAG=dev HEADWAY_DATA_TAG=dev bin/generate seattle.env configs/seattle-dev
HEADWAY_CONTAINER_TAG=dev bin/generate planet.env configs/planet-dev
bin/generate planet.env configs/planet
