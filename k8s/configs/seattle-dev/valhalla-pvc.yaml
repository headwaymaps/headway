apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valhalla-seattle-latest-dev
  labels:
    app.kubernetes.io/part-of: headway
spec:
  accessModes: [ "ReadWriteOnce" ]
  resources:
    requests:
      storage: 200Gi
