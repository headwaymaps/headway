name: Push

on:
  push:
    branches: [dev]

jobs:
  build:
    name: Push dev images to ghcr.io
    runs-on: ubuntu-latest
    steps:
      - uses: earthly/actions-setup@v1
        with:
          version: "0.7.1"
      - uses: actions/checkout@v2
      - name: Docker login
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u headwaymaps --password-stdin
      - name: Earthly version check
        run: earthly --version
      - name: Push dev images
        run: earthly --use-inline-cache --save-inline-cache --push +images --tags=dev
      - name: Push dev branded images
        run: earthly --use-inline-cache --push +images --branding=maps.earth --tags "maps-earth-dev"
